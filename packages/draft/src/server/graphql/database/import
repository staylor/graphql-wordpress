collections=("user" "post" "media" "video" "term" "taxonomy" "show" "settings")
for i in "${collections[@]}"
do
  mongoimport --host $MONGO_HOST --ssl -u $MONGO_USER -p $MONGO_PASSWORD --authenticationDatabase admin --db $1 --collection $i --file highforthis.$i.json
done
