query TaxonomyQuery {
  taxonomies @connection(key: "taxonomies") {
    count
    edges {
      node {
        id
        name
        slug
        description
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
