query ShowQuery(
  $first: Int
  $after: String
  $date: Float
  $artist: ObjID
  $venue: ObjID
  $search: String
) {
  shows(
    first: $first
    after: $after
    date: $date
    artist: $artist
    venue: $venue
    search: $search
  ) @connection(key: "shows", filter: ["date", "artist", "venue", "search"]) {
    count
    edges {
      node {
        id
        title
        date
        artist {
          id
          name
        }
        venue {
          id
          name
        }
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
