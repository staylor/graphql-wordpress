import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 2,
    backgroundColor: '#fff',
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    padding: 10,
  },
});
