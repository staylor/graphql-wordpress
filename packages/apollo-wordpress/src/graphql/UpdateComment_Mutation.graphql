#import "./Comment_comment.graphql"

mutation UpdateComment_Mutation($input: UpdateCommentInput!) {
  updateComment(input: $input) {
    comment {
      ...Comment_comment
    }
    cookies
    status
  }
}
